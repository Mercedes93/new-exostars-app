// src/Settings.js
import React from 'react';
import './styles/App.css';

const Settings = () => {
  return (
    <div>
      <header>Settings</header>
      <p style={{ padding: '20px' }}>Adjust app preferences here...</p>
      {/* Add settings options like theme, display, etc. */}
    </div>
  );
};

export default Settings;

